#!/bin/bash
#----------------------------------------------------------
#-- Environment variables common to all SBMT_FILES --------
#-- They can be common directories, common variables etc. -
#----------------------------------------------------------
#-- Some bash commands are assigned to variables ----------
export TEMP=TeMPLaTe; export COPY='cp -f'
export REMOVE='rm -f'; export LINK='ln -sf'
export MOVE='mv -f'; export MKDIR='mkdir'

export USRNAME=$(id -u -n)
export USRHOME=~${USRNAME}
#-- IO directories ----------------------------------------
#-- Static I/O paths --------------------------------------
export WRKPATH=/y1/aydogdu
export REMPATH=/Volumes/sim
export PRJPATH=${WRKPATH}/REDDA
export GITPATH=${WRKPATH}/GIT
export MODPATH=${GITPATH}/nextsim
export MSHPATH=${MODPATH}/mesh
export OUTPATH=${PRJPATH}/DATA/neXtSIM_out
export SCRPATH=${MODPATH}/scripts
export RUNPATH=${SCRPATH}/ensemble
export REMLINK=${REMPATH}/aydogdu/nextsim/data/data_links
export FORCLOC=${REMPATH}/data
export FORCREM=/Data/sim/data
#-- Input files -------------------------------------------
DYNFILE=neXtSIM_ensemble.env
ENVFILE=neXtSIM.env
CONFILE=nextsim.cfg
RUNFILE=run_docker_nextsim_bangkok.sh
LOGFILE=nextsim.log


##-- Set experiment parameters here  -----------------------
## RNLEN is number of time step; use for neXtSIM
#export RNLEN=1800
## TSTEP is seconds in time step
#export TSTEP=12
## RNSEC is seconds to run; use for DART
#export RNSEC=$( echo "${RNLEN} * ${TSTEP}" | bc )
#export CYCLE=${RNSEC}
##-- Set the common directories here  ----------------------
#export EnKFPATH=${USRHOM}/EnKF/neXtSIM/models/neXtSIM/work
#export DIAPATH=${USRHOM}/neXtSIM_POSTPROC/MESH_READ
#export LOGPATH=${WRKPATH}/LOG
#export CHECKFILE=${WRKPATH}/submitcheck.lst
#export MODPRE=${USRHOM}/neXtSIM_PREPROC
#export MODINI=${MODPRE}/ENSEMBLE_IC
#export OBSSEQ=${EnKFPATH}/obs_seq.ferrybox
